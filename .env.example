# This app has no backend and currently requires no environment variables —
# all video processing happens client-side in the browser. This file is a
# placeholder for future use (e.g. analytics keys) so the convention exists
# if/when one is needed.
